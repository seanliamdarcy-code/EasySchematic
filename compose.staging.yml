services:
  easyschematic-staging:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8081:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - TATESIDE_API_PORT=8789
    restart: unless-stopped
