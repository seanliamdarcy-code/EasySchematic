services:
  easyschematic-staging:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8081:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
